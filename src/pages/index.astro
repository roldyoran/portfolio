---
import Layout from "@/layouts/layout.astro";
import Home from "@/components/home.astro";
import config from "@/data/content";
---

<Layout title={`${config.name} — ${config.titleEs ?? config.title}`} lang="es">
  <Home lang="es" />
</Layout>
